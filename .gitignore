node_modules/
dist/
build/
.next/
coverage/
.env
.env.local
*.tsbuildinfo

# The Android app was discontinued and its source removed; this only hides any
# leftover local build output in a mobile/ folder.
mobile/

# Dev server logs — noisy, machine-specific, and can contain request data.
*devserver.log
*.log

# Local tooling state, not part of the project.
.claude/

# The pre-rewrite Laravel / NestJS codebase. Superseded by backend/ and web/,
# no longer builds, and kept on disk only for reference.
_archive_legacy/

# OS cruft
.DS_Store
Thumbs.db
