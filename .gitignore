node_modules/
dist/
build/
.next/
coverage/
.env
.env.local
*.tsbuildinfo

# Flutter / Dart
mobile/.dart_tool/
mobile/.flutter-plugins
mobile/.flutter-plugins-dependencies
mobile/.pub-cache/
mobile/.pub/
mobile/build/
mobile/android/.gradle/
mobile/android/local.properties
mobile/ios/Flutter/.last_build_id

# Dev server logs — noisy, machine-specific, and can contain request data.
*devserver.log
*.log

# Local tooling state, not part of the project.
.claude/

# The pre-rewrite Laravel / NestJS codebase. Superseded by backend/, web/ and
# mobile/, no longer builds, and kept on disk only for reference.
_archive_legacy/

# OS cruft
.DS_Store
Thumbs.db
